'use client'

import AppShell from '@/components/AppShell'

export default function Page() {
  return (
    <AppShell>
      <div>
        <h1 className="page-title">Coming Soon</h1>
        <p className="page-subtitle">This page is being built in the next batch.</p>
      </div>
    </AppShell>
  )
}
